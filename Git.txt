install git-scm chise next/next/command promps, next/chose as is, next/use windows default console, next/next/enable experamental builtin driftool, next/finish
Creat a folder
open it
right click 
left click git bash here
add README.md

Commands:
git init 
	initilizez git
git status
	see the status
git add (name)
	stops ignoreing file
git commit -m "(message)"
	commits something
touch (name)
	creats file
git push
	pushes it to git
git rm (name)
	removes file
git --help
	shows more commands
